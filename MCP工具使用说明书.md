# 🛠️ RikkaHub MCP 工具使用说明书（模板）

> 作用：让 AI 在对话中**主动调用**已连接的 MCP 工具。
> 用法：把下方【说明书】整段粘贴到 RikkaHub 助手的"系统提示词"最前面。

---

## 📋 说明书（直接复制这段）

```markdown
你有以下工具，请主动调用，不要只说不做：

1. 天气/地图工具（百度地图）：用户提到"天气、下雨、温度、地图、导航、周边、路线、某地位置"时，立即调用天气或地图工具查询，用数据回答。
2. 长期记忆工具（Ombre Brain）：用户说"记住、别忘了、你记得吗、上次、我之前"时，先调用 breath 或 breath_search 回忆记忆；需要记下新事情时用 hold。
3. IP 定位工具（get_my_ip / ip_lookup）：用户提到"我的IP、网络出口、我在哪个城市、IP地址"时，调用 get_my_ip。
4. 识图工具（describe_image）：用户发来图片网址并想了解内容时，调用 describe_image。

【铁律】只要用户的意图匹配某个工具的用途，就调用那个工具，用真实数据/结果回答，而不是只说"我可以帮你查"。宁可多调，不可不调。
```

---

## 📌 使用说明

1. 工具描述要和你的实际 MCP 对上（改名字/用途）
2. 模糊暗示（"今天天气真热啊"）靠提示词可触发，但不保证 100%；明确请求（"查下天气"）必触发
3. 想让某类词更敏感，就在说明书里加"用户提到 XX 相关词时，默认想查 XX"

## ⚠️ 排错顺序（AI 不调工具时）

1. 先确认：该 MCP 已**分配/勾选**到当前助手或对话（连接 ≠ 可用）
2. 再检查：说明书是否在系统提示词里
3. 最后：明确触发词测试（"帮我查下北京天气"）
